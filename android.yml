name: Android CI

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ✅ Gradle 7.0.2 يحتاج JDK 11
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Grant execute permission
        run: chmod +x gradlew

      # استرجاع الأيقونات إذا موجود icons.zip
      - name: Restore launcher icons from icons.zip
        if: hashFiles('icons.zip') != ''
        run: |
          unzip -o icons.zip -d app/src/main/res/

      # ✅ Build Release APK (صغير + سريع)
      - name: Build Release APK
        run: ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: mashaf-release-apk
          path: app/build/outputs/apk/release/*.apk